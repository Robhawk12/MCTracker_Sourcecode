<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Massage Client Tracker"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MctrackerRebuild"
        tools:targetApi="31">


        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.ClientListActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false"
            android:label="My Clients"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.ClientDetailsActivity"
            android:theme="@style/Theme.AppCompat"
            android:exported="false"
            android:label="Client Details"
            android:parentActivityName=".ui.MainActivity" />
    </application>


</manifest>